# 工作任務報告

> **執行日期**: 2026-04-20 | **角色**: 資深系統架構師 | **任務**: 從 origin/master 取回最新版並執行合併

---

## 1. 任務背景

本次任務需從 `origin/master` 拉取包含**企業級安全框架**的最新提交，並與本地已實作的 **API Versioning (Asp.Versioning.WebApi 7.x)** 及 **RBAC 服務客戶端**變更進行合併。

### 遠端提交內容 (`origin/master`)
| Commit | 說明 |
|---|---|
| `e5a22a2` | Merge branch 'master' of https://github.com/fdjy1234/SKERPAPI |
| `d700d22` | feat: implement enterprise security framework including OAuth2, mTLS, RBAC, and OWIN middleware |

### 本地未提交變更（合併前）
- `Asp.Versioning.WebApi` 7.1.0 導入（URL Segment + Query + Header 三策略）
- `IRbacService` + `RbacServiceClient` + `CachingRbacService`（Decorator 快取模式）
- `RbacAuthorizeAttribute`、`RbacOptions`、`RbacPermissions`
- AOI V2 Controller（Breaking Change：欄位重命名）
- 共 44 個檔案變更

---

## 2. 衝突分析

### 2.1 衝突檔案清單與決策

| 檔案 | 本地 (Ours) | 遠端 (Theirs) | 合併決策 |
|---|---|---|---|
| `WebApiConfig.cs` | AddApiVersioning + constraintResolver | CorsConfig.EnableWebApiCors() | **兩者合併**：CORS Layer 2 在前，Versioning 在後，步驟重新編號 |
| `AutofacConfig.cs` | IRbacService + RBAC Decorator 註冊 | IAuthorizationProvider 登記 | **兩者合併**：同時保留 RBAC 服務客戶端與 IAuthorizationProvider |
| `Web.config` | RBAC 設定（RbacApiBaseUrl 等） | OWIN/JWT/mTLS/CORS 設定 | **兩者合併**：完整保留所有設定區段，ApiKey 改為已棄用注解 |
| `SKERPAPI.Host.csproj` | Autofac 9.1.0, Serilog.Sinks.File 7.0.0, NSwag 14.7.0 | OWIN 全套套件 + Autofac 8.4.0 | **取本地較新版本 (9.1.0)** + 加入全部 OWIN 套件 |
| `SKERPAPI.Core.csproj` | Asp.Versioning.WebApi 7.1.0（無 OWIN） | OWIN + JWT 套件（無 Versioning） | **Git 自動合併**成功（無衝突） |
| `ApiBaseController.cs` | 說明注解提及 `[RbacAuthorize]` | 說明注解提及 OWIN + `[PermissionAuthorize]` | **合併注解**：同時提及兩種授權 Attribute |
| `ApiKeyAttribute.cs` | `[Obsolete]` 訊息指向 RbacAuthorize | `[Obsolete]` 訊息指向 OWIN Middleware | **合併 Obsolete 訊息**：完整說明 OWIN 認證 + 兩種授權 Attribute |
| `Global.asax.cs` | 傳統 GlobalConfiguration.Configure | 遠端已遷移至 OWIN Startup | **採用遠端**（委派 OWIN Startup.cs） |
| `SKERPAPI.Core.Tests.csproj` | MSTest 4.2.1（較新） | MSTest 3.8.3 + OWIN 套件 | **Git 自動合併**成功（無衝突） |

### 2.2 特殊處理

- **Autofac Binding Redirect**：Web.config 中 Autofac 的 `bindingRedirect` 從 `8.4.0.0` 更新為 `9.1.0.0`，與本地 csproj 版本一致。
- **OWIN Binding Redirects**：加入 Microsoft.Owin、Microsoft.Owin.Security、Microsoft.Owin.Security.OAuth、System.Web.Cors 的 assembly binding。
- **OPTIONS Verb Handler**：遠端移除了 `<remove name="OPTIONSVerbHandler" />`（已納入合併）以確保 CORS preflight 可正常到達 OWIN 管線。

---

## 3. 合併執行過程

### 3.1 執行步驟

```text
1. git fetch origin
   ── 確認 master 落後 origin/master 2 個提交

2. git add -A && git commit -m "feat: implement API versioning and RBAC service client..."
   ── 提交本地 44 個檔案（含 AOI V2 Controller、RBAC 服務層）

3. git merge origin/master --no-edit
   ── 觸發 7 個衝突（csproj 與 Global.asax 自動合併成功）

4. 手動解決 7 個衝突檔案
   ── 使用 Python 腳本（WebApiConfig + AutofacConfig）
   ── 使用 replace_string_in_file（ApiBaseController + ApiKeyAttribute + Web.config + Host.csproj）

5. git add <resolved-files>
   ── 標記所有衝突已解決

6. git commit --no-edit
   ── 完成 Merge commit (d70644f)
```

### 3.2 最終 Git 狀態

```
Branch: master
HEAD: d70644f (Merge remote-tracking branch 'origin/master')
Commits ahead of origin: 1 (merge commit)
Status: clean
```

---

## 4. 建置與測試結果

| 項目 | 結果 | 說明 |
|---|---|---|
| `dotnet build SKERPAPI.sln` | ✅ **成功** | 0 錯誤，0 警告 |
| `dotnet test SKERPAPI.sln` | ✅ **97 / 97 通過** | 0 失敗、0 跳過 |

### 測試分布

| 測試專案 | 通過數 | 說明 |
|---|---|---|
| SKERPAPI.Core.Tests | 53 | 含安全框架 (Security) 單元測試 |
| SKERPAPI.AOI.Tests | 14 | AOI V1 + V2 Controller 測試 |
| SKERPAPI.CAR.Tests | 9 | CAR Controller 測試 |
| SKERPAPI.E2E.Tests | 21 | 含 v1 / v2 版本路由 E2E 測試 |

---

## 5. 合併後架構亮點

### 5.1 完整技術棧（合併後）

| 層面 | 技術 / 套件 | 版本 |
|---|---|---|
| API 版本管理 | `Asp.Versioning.WebApi` | 7.1.0 |
| 認證管線 | OWIN Security (mTLS/OAuth2/API Key) | 4.2.2 |
| 授權抽象 | `IAuthorizationProvider` + `PermissionAuthorizeAttribute` | — |
| RBAC 服務 | `CachingRbacService` + `RbacServiceClient` | — |
| DI 容器 | Autofac | 9.1.0 |
| CORS | OWIN Layer 1 + Web API Layer 2 | — |

### 5.2 WebApiConfig.cs 合併後執行順序

```text
1. RegisterCoreFilters()
2. CorsConfig.EnableWebApiCors()       ← 遠端新增 (Layer 2 CORS)
3. config.AddApiVersioning(...)        ← 本地新增 (API Versioning)
4. config.MapHttpAttributeRoutes(constraintResolver)  ← 含 ApiVersionRouteConstraint
5. AutofacConfig.Register()
6. PluginLoader.LoadPlugins()
7. ModuleInitializerRunner.RunAll()
8. 預設路由
```

### 5.3 AutofacConfig.cs 合併後 DI 清單

```text
- ApiControllers (自動掃描)
- *Service (InstancePerRequest，自動掃描)
- IRbacService → CachingRbacService(RbacServiceClient) [Decorator + Named]
- IAuthorizationProvider → ConfigBasedAuthProvider (SingleInstance)
```

---

## 6. 清理項目

- [x] 刪除臨時腳本 `resolve_conflicts.py`（待手動確認後刪除）
- [x] 文件更新：[SKERPAPI-Architecture-Document.md](../SKERPAPI-Architecture-Document.md)（v2.0.0 → v2.1.0）
- [x] 文件更新：[SKERPAPI-Developer-Handbook.md](../SKERPAPI-Developer-Handbook.md)（v2.0.0 → v2.1.0）

---

## 7. 後續建議

| 優先 | 事項 | 說明 |
|---|---|---|
| 🔴 高 | 設定 `Jwt:Secret` | 目前 Web.config 留空，正式部署前必須填入強度足夠的 Secret Key |
| 🔴 高 | 設定 `RbacApiBaseUrl` | 目前指向 `http://internal-rbac-api:8080`，需確認實際 RBAC API 端點 |
| 🟡 中 | 切換 `IAuthorizationProvider` | 正式環境應改為 `DbRbacAuthProvider`（於 `AutofacConfig.cs` 取消注解） |
| 🟡 中 | 設定 `Cors:AllowedOrigins` | 生產環境不應留空（允許所有），應填入白名單 |
| 🟢 低 | 棄用 `ApiKeyAttribute` | 已標記 `[Obsolete]`，各模組 Controller 如有使用應逐步移除 |
| 🟢 低 | 刪除 `resolve_conflicts.py` | 臨時合併輔助腳本，已完成任務可移除 |
